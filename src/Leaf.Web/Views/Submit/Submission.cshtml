@model Leaf.Web.Models.Submit.SubmissionViewModel

@{
    ViewBag.Title = "Submission";
}

<h2>Нов въпрос</h2>

<p>@Model.Condition</p>
<p>@Model.CorrectAnswer</p>
@foreach (var incorrectAnswer in Model.IncorrectAnswers)
{
    <p>@incorrectAnswer</p>
}